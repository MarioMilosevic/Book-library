'use strict'

export const toggleClass = (el, cl) => {
    el.forEach(element => element.classList.toggle(cl));
}

const nekiObjekat = {
    title: 'Neka njiga',
    author: 'Mario',
    broj: '300'
}

